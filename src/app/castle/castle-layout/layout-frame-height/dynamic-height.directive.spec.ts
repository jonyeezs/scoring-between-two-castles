import { DynamicHeightDirective } from './dynamic-height.directive';

describe('DynamicHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHeightDirective();
    expect(directive).toBeTruthy();
  });
});
